# Unique project key (as seen in your SonarQube project URL)
sonar.projectKey=pallamadhuri486-group_Boardgame-main_AZh9UqL-OBji3ssjNsSg

# Project name for display in the SonarQube UI
sonar.projectName=Boardgame

# Project version (optional)
sonar.projectVersion=1.0

# Directory where source code exists
sonar.sources=.

# Directory where compiled binaries are located (Java only)
sonar.java.binaries=.

# Optional: Fail the analysis if Quality Gate fails (useful in CI/CD)
sonar.qualitygate.wait=true

# SonarQube server URL - Uncomment if scanning locally
sonar.host.url=http://localhost:9000
